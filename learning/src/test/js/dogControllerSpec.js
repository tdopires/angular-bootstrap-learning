describe('PetShop controllers', function() {
	describe('DogCtrl', function(){
		it('should had no dogs list', function() {
			var scope = {};
			scope.dogs = null;
			
			expect(scope.dogs).toBe(null);
			expect(scope.dogs).toBe(null);
		});
	});
});
